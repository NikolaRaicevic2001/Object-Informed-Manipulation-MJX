<mujoco model="xarm6_pusht_live_real">
  <!-- Checked-in placeholder: an empty table, identical to
       open_table_real.xml. oim.worlds.real3d.live_scene.write_live_real_xml
       OVERWRITES this file at the start of any run that passes
       --obstacle-calibration with --scene live_real, with exactly the
       obstacles that resolved that run -- see that module's docstring.
       Running --scene live_real with no --obstacle-calibration at all
       (or before ever generating it) uses this file as-is, i.e. the bare
       table. -->
  <include file="common_real.xml"/>
  <include file="tee_real.xml"/>
  <worldbody>
    <camera name="front" pos="1.4907 0.3750 0.8684" xyaxes="0.0000 1.0000 -0.0000 -0.4226 0.0000 0.9063"/>
  </worldbody>
</mujoco>
